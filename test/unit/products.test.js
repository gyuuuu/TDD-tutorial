describe("Calculation", ()=> {
    test('tow plus two is four', () => {
        expect(2 + 2).toBe(4);
    })
    
    test('tow plus two is not five', () => {
        expect(2 + 2).not.toBe(5);
    })
})
